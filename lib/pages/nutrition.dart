import 'package:flutter/material.dart';

class Nutrition extends StatelessWidget {
  const Nutrition({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("🥗 Nutrição Page", style: TextStyle(fontSize: 24)));
  }
}